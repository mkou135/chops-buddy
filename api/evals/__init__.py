"""Golden-case eval suite for the LLM session-proposal task.

Cases live in evals/cases/<category>/<slug>.json. Results are appended to
evals/results/ and never edited. Run with `python -m evals`.
"""
